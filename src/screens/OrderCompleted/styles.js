import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  page: {
    flex: 1,
    backgroundColor: 'white',
  },

  container: {
    margin: 15,
    height: '100%',
    alignItems: 'center',
  },

  checkMarkAnimationContainer: {
    height: 100,
    alignSelf: 'center',
    marginBottom: 30,
  },

  restaurantNameText: {
    fontSize: 20,
    fontWeight: 'bold',
  },

  cookingAnimationContainer: {
    height: 200,
    alignSelf: 'center',
  },
});

export default styles;
